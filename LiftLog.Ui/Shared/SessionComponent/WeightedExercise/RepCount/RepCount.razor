<button class="w-12 h-12  rounded-xl text-center flex justify-center align-middle transition-colors duration-75 items-center @ColorClass @RepCountToStartClass" @onclick="CycleRepCount">
    <span class="font-bold">@(RepCountValue?.ToString()??"-")</span>
    <span class="text-xs inline align-text-top">/@MaxReps</span>
</button>

@code {

    [Parameter]
    public int MaxReps { get; set; }

    [Parameter]
    public int? RepCountValue { get; set; }

    [Parameter]
    public Action CycleRepCount { get; set; } = null!;

    [Parameter]
    public bool ToStartNext { get; set; }

    private string ColorClass => ToStartNext || RepCountValue is not null ? "bg-red-500 text-white" : "bg-gray-200 text-gray-500";

    private string? RepCountToStartClass => ToStartNext ? "animate-pulse" : null;
}