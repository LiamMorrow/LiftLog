region                     = "eu-west-1"
liftlog_backup_bucket_name = "liftlog-backups" # this must be globally unique
profile                    = "liftlog-aws-profile"
#delete_after_days = 365 # uncomment this otherwise the default will be to keep backups forever