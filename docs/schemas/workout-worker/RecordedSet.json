{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "repsCompleted": {
      "type": "integer"
    },
    "completionDateTime": {
      "$ref": "./OffsetDateTime.json"
    },
    "power": {
      "type": "integer",
      "description": "Peak power in whole watts achieved during the set, as reported by equipment with a power readout (e.g. Keiser functional trainers)."
    }
  },
  "required": [
    "repsCompleted",
    "completionDateTime"
  ]
}